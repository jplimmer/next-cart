import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'api.lorem.space',
      'placeimg.com',
      'i.imgur.com',
      'picsum.photos',
      'api.escuelajs.co',
    ],
  },
};

export default nextConfig;
